*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
# Firebase console config (per-project; carries the Firebase API key) — kept local like local.properties.
# Without it the build skips the Firebase plugins and the app runs Firebase-free (see app/build.gradle.kts).
/app/google-services.json
/app/src/*/google-services.json
