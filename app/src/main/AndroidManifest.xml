<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Saving the share image to the gallery: only pre-Android-10 needs this (scoped storage on Q+). -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <!-- Retirement alerts (Settings → Notifications): runtime-granted on Android 13+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- NO advertising identifier. BrickWares shows no ads, but firebase-analytics merges these three
         permissions into the build by default; with them present the Play Data Safety form has to
         declare ad-ID access and the privacy policy's "no advertising identifier" would be false.
         Removed here; ad-ID collection is also switched off in the <application> meta-data below, so
         opting into usage analytics never involves the ad ID. Check after a Firebase bump:
         app/build/intermediates/merged_manifest/<variant>/…/AndroidManifest.xml must not list them. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION" tools:node="remove" />

    <application
        android:name=".BrickWaresApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BrickWares">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:label="@string/app_name"
            android:theme="@style/Theme.BrickWares.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Serves the rendered "share your collection" image to the chosen app via a content:// URI. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Usage analytics is OPT-IN (Settings → Privacy → Usage analytics): its auto-collection is off and
             Observability.applyConsent() enables it only after the user agrees. Crashlytics is ALWAYS on
             (crash diagnostics) — no meta-data here, so it defaults to enabled. -->
        <meta-data android:name="firebase_analytics_collection_enabled" android:value="false" />
        <!-- Even when the user opts in: never the advertising ID, never ad-personalization signals. -->
        <meta-data android:name="google_analytics_adid_collection_enabled" android:value="false" />
        <meta-data android:name="google_analytics_default_allow_ad_personalization_signals" android:value="false" />
    </application>

</manifest>