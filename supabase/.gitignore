# Supabase
.branches
.temp

# dotenvx
.env.keys
.env.local
.env.*.local

# BrickWares: local secrets + generated catalog seed (regenerate via scripts/fetch-catalog.mjs)
.env
seed.sql

# Local seed backups + ad-hoc test SQL (scratch, never committed)
seed.backup.sql
seed.prev.sql
retired_test.sql

# Studio scratch queries (local only)
snippets/
seed.sql.bak*
seed.sql.*-bak

# Generated by `scripts/fetch-catalog.mjs --enrich` — applied to a DB, then disposable
enrich-*.sql
